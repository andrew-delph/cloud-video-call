{
  "name": "matcher",
  "scripts": {
    "lint": "eslint --ext .js,.ts .",
    "build": "tsc",
    "build:watch": "tsc --watch",
    "dev": "nodemon --delay 1000ms --ignore dist --watch ./src  --ext ts --exec 'npm run k8-restart'",
    "docker": "docker build -f Dockerfile -t andrewdelph/video-call-matcher:latest ../ && docker push andrewdelph/video-call-matcher:latest",
    "k8-restart": "npm run docker && kubectl rollout restart deployment/matcher && kubectl rollout status deployment/matcher --watch=true --timeout=60s  && date",
    "prod": "node ./dist/match-worker.js"
  },
  "engines": {
    "node": "16"
  },
  "main": "lib/index.js",
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^18.15.3",
    "@types/uuid": "^9.0.1",
    "tsc-watch": "^6.0.0",
    "typescript": "4.9.4"
  },
  "dependencies": {
    "express": "^4.18.2",
    "firebase-admin": "^11.4.1",
    "uuid": "^9.0.0",
    "winston": "^3.8.2",
    "common": "link:../common"
  },
  "private": true
}
