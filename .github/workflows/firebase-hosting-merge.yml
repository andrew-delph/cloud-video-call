# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
on:
  workflow_run:
    workflows: ["Version Flutter"]
    types:
      - completed
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-java@v1
        with:
          java-version: "14.0.1"
      - uses: subosito/flutter-action@v1
        with:
          flutter-version: "3.3.10"
      - name: Get dependencies
        run: flutter pub get
        working-directory: flutter_app
      - name: Build app
        run: flutter build web --dart-define="SOCKET_ADDRESS=https://react-video-call-fjutjsrlaa-uc.a.run.app"
        working-directory: flutter_app
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_REACT_VIDEO_CALL_THING72 }}"
          projectId: react-video-call-thing72
          channelId: live
